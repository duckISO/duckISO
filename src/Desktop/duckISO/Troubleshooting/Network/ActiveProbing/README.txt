ActiveProbing intermittently pings Microsoft's Servers to check for a network connection.
It may cause the network icon to display a disconnected icon and cause some apps to report no network connectivity whilst disabled.

It is enabled by default, but you can disable it if you want to.